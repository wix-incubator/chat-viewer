/**
 * An index constant that indicates a not found state.
 * @internal
 */
export const IDX_NOT_FOUND = -1 as const; 
/**
 * A prefix ID used in the chat viewer.
 * @internal
 */
export const PREFIX_ID = 'chat-viewer-prefix-id' as const;
/**
 * A suffix ID used in the chat viewer.
 * @internal
 */
export const SUFFIX_ID = 'chat-viewer-suffix-id' as const;